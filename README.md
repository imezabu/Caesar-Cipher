# Allows user to encrypt and decrypt text using a Caesar Cipher
# Format
./your_executable text_string shift_value command_string

if a user leaves arguments blank then program will prompt user for them
